name: PR 100 finalize behavioral investigation (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Serial finalizer replaced by parallel evidence and validation workflow."
