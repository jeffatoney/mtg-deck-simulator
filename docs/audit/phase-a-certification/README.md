# Durable Phase A certification

`CERTIFICATION.json` is created from a GitHub Actions verification candidate and checked
on every later pull request and push to `main`. Local reproduction may inform debugging,
but it cannot create the authoritative record.
