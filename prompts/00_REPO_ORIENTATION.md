Read `AGENTS.md` and every file under `docs/`. Inspect the repository without modifying files or running any simulation.

Return:

1. Current repository structure and existing implementation status
2. Missing source-of-truth files or contradictions
3. Open decisions that block implementation or pilot execution
4. A proposed sequence of focused pull requests
5. A traceability plan mapping every requirement to a module and test

Do not write code, do not fetch live card data, and do not claim that any prior chat test or simulation is evidence. Base the report only on repository contents and commands you actually run.
