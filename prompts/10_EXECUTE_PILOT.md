Execute the coded pilot only after the dry-run manifest has been reviewed and every blocking decision is resolved. Do not run the full 25,000-game study.

Procedure:

1. Verify a clean commit and rerun all source, lint, type, unit, property, integration, and competency checks.
2. Run policy discovery on the frozen discovery seeds using the precommitted policy matrix and advancement rule.
3. Select finalists using discovery results only.
4. Evaluate every finalist on the untouched validation seeds.
5. Lock a preliminary standard policy and run 500 canonical standard games.
6. Run 200 exploratory games paired with canonical Standard Games 1–200.
7. Generate the required audit sample, adding supplemental audit-only scenarios only when a required category has fewer than ten examples.
8. Save all raw events, manifests, counts, metrics, paired differences, and decoded games.

Do not describe percentages as final deck conclusions. Report base-seed count, policy-evaluation count, canonical-game count, exploratory-game count, and any incomplete shard separately.
