This prompt is intentionally locked. Do not execute it unless the user explicitly says to run the full study after accepting a valid pilot.

When authorized, first create a full-study dry-run manifest for 20,000 standard canonical games and 5,000 separately reported exploratory games, with deterministic shards, seed lists, configs, expected policy/search counts, audit sampling, resource estimates, and aggregation checks. Seek review of that dry-run manifest before executing the full study.

Never combine standard and exploratory percentages. Never reuse invalid pilot artifacts as full-study evidence.
